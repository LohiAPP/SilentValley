import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    template: "%s | Silent Valley",
    default: "Silent Valley - The Sacred Meditation Ashram",
  },
  description: "A sacred spiritual space to discover the silence within. Founded by Prakruthi Umamahesh.",
  openGraph: {
    title: "Silent Valley - The Sacred Meditation Ashram",
    description: "A sacred spiritual space to discover the silence within.",
    url: "https://silentvalley.org",
    siteName: "Silent Valley",
    images: [{ url: "/og-image.jpg", width: 1200, height: 630 }],
    locale: "en_US",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Silent Valley - The Sacred Meditation Ashram",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${playfair.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
